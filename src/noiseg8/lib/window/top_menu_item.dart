import 'package:arna/arna.dart';
import 'package:fluent_ui/fluent_ui.dart';
import 'package:noiseg8/window/top_menu_item_buttons.dart';

class top_menu_item extends StatelessWidget {
  const top_menu_item({
    Key? key,
  }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return top_menu_item_buttons();
  }
}
